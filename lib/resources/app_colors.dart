import 'package:flutter/material.dart';
import 'package:zens_test/main.dart';

class AppColors {
  static Color primaryColor =
      Theme.of(StateManager.navigatorKey.currentContext!).colorScheme.onPrimary;
}
